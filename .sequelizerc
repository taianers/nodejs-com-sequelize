const path = require('path');

module.exports = {
    config: path.resolve(__dirname, 'src', 'config', 'database.js'), 
    'migration-path': path.resolve(__dirname, 'src', 'database', 'migrations'),
};